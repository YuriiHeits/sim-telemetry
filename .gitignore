# Build artifacts
_build/
__pycache__/
*.pyc
*.spec

# Telemetry data — generated logs, large and ever-growing
*.csv

# Built binary — rebuild from stint_logger.pyw via PyInstaller (see README)
StintLogger.exe

# Per-user logger settings (fuel plan minutes)
logger.cfg
